import React, { Component } from 'react';
import TabList from './TabList';

class Main extends Component {
  render() {
    return (
      <main className = "main">
        <TabList/>
      </main>
    );
  }
}

export default Main;
